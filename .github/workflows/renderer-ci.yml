name: "@inklyre/oes-renderer CI"

on:
  push:
    branches: [main]
    paths:
      - "packages/renderer/**"
      - "packages/core/**"
      - ".github/workflows/renderer-ci.yml"
  pull_request:
    paths:
      - "packages/renderer/**"
      - "packages/core/**"
      - ".github/workflows/renderer-ci.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build @inklyre/oes-core
        run: npm run build --workspace @inklyre/oes-core

      - name: Typecheck
        run: npm run typecheck --workspace @inklyre/oes-renderer

      - name: Lint
        run: npm run lint --workspace @inklyre/oes-renderer

      - name: Test
        run: npm run test --workspace @inklyre/oes-renderer

      - name: Build
        run: npm run build --workspace @inklyre/oes-renderer
